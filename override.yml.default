services:
  x-gpu-none: {}
  x-gpu-nvidia:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              capabilities:
                - gpu

  x-jellyfin:
    extends: x-gpu-${GPU:-none}

  x-nextcloud:
    extends: x-gpu-${GPU:-none}
